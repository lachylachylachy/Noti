import { PlaceholderSpace } from "./PlaceholderSpace";

export function ArchiveWorkspace() {
  return (
    <PlaceholderSpace
      eyebrow="Archive"
      title="Quiet storage for old threads."
      description="Completed notes, archived projects, and old context can stay accessible without cluttering the main workspace."
    />
  );
}
